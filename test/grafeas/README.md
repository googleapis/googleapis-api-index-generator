This directory only exists to avoid having to change the
`generate-index.sh` script to cope with its absence when running the
test.

This *file* only exists as Git doesn't register empty directories...